/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/testing_meyda", // This is for Github pages
    output: "export",
    reactStrictMode: true,
};

export default nextConfig;
